CREATE TABLE data (id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,latitude DECIMAL(10,8) NOT NULL,longitude DECIMAL(11,8) NOT NULL,humidity FLOAT NOT NULL, pressure FLOAT NOT NULL ,temperature FLOAT NOT NULL,date_time TIMESTAMP NOT NULL);
